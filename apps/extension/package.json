{
  "name": "@check-grammar/extension",
  "version": "0.1.0",
  "private": true,
  "description": "MV3 overlay: on-device engine as you type",
  "scripts": {
    "build": "node build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
